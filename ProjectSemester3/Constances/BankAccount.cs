using System;

public class BankAccountConstances
{
    public static TimeSpan Duration = new TimeSpan(365 * 2, 0,0,0,0);
    public static TimeSpan MinimumDuration = new TimeSpan(30 * 6, 0, 0, 0, 0); //days
}
